{% extends 'base.html.twig' %}

{% block body %}
    <h2>Dodaj szkołę</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Dodaj"   class="btn btn-default"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('school_index') }}"  class="btn btn-default">Powrót</a>
        </li>
    </ul>
{% endblock %}
